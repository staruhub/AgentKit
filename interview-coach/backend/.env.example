# Interview Coach Environment Configuration
# Copy this file to .env and fill in your values

# API Configuration
API_HOST=0.0.0.0
API_PORT=8000

# Model Configuration (for demonstration)
# Using Claude models via Claude Agent SDK
DEFAULT_MODEL=sonnet

# Demo Mode (limits interview to 1-2 rounds)
DEMO_MODE=true
MAX_INTERVIEW_ROUNDS=2
